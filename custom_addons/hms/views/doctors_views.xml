<odoo>
    <menuitem id="hms_menu_doctors" name="Doctors" parent="hms_menu_root"/>

    <record id="action_hms_doctors" model="ir.actions.act_window">
        <field name="name">Doctors</field>
        <field name="res_model">hms.doctors</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_hms_doctors_list_view" model="ir.actions.act_window.view">
        <field name="sequence" eval="10"/>
        <field name="view_mode">list</field>
        <field name="act_window_id" ref="action_hms_doctors"/>
    </record>

    <record id="action_hms_doctors_form_view" model="ir.actions.act_window.view">
        <field name="sequence" eval="20"/>
        <field name="view_mode">form</field>
        <field name="act_window_id" ref="action_hms_doctors"/>
    </record>

    <menuitem id="hms_menu_doctors_action" name="Manage Doctors"
              parent="hms_menu_doctors" action="action_hms_doctors"/>

    <record id="view_hms_doctors_list" model="ir.ui.view">
        <field name="name">hms.doctors.list</field>
        <field name="model">hms.doctors</field>
        <field name="arch" type="xml">
            <list>
                <field name="first_name"/>
                <field name="last_name"/>
            </list>
        </field>
    </record>

    <record id="view_hms_doctors_form" model="ir.ui.view">
        <field name="name">hms.doctors.form</field>
        <field name="model">hms.doctors</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_add_doctor" type="object" string="Add" class="btn-primary" invisible="id"/>
                    <button name="action_update_doctor" type="object" string="Update" invisible="not id"/>
                </header>
                <sheet>
                    <group>
                        <field name="first_name"/>
                        <field name="last_name"/>
                        <field name="image"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
